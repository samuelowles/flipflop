# Dependencies
node_modules/

# Build output
dist/
.wrangler/

# Environment & secrets
.env
.env.*
!.env.example

# Logs
*.log

# Python
__pycache__/
.pytest_cache/
*.pyc
*.pyo
.venv/
venv/

# Ruflo runtime (local agent state, not project code)
.claude-flow/
.swarm/
ruvector.db

# Temp files
*.tmp.*
*~

# Powerswitch live-capture tooling (#240). Playwright is used to capture the
# real wire protocol; it is NEVER a project dependency (see scripts/capture-powerswitch.mjs).
# scripts/.playwright/ holds the local Playwright JS install; probe-* are throwaway.
scripts/.playwright/
scripts/probe-powerswitch.mjs
scripts/probe-out.txt

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Powerswitch capture harness — local Playwright install + run log (issue #240)
scripts/.playwright/
scripts/capture-log.txt

# GLM transient artifacts (not source)
.claude/worktrees/
.tmp-issues/
scripts/probe*.mjs
scripts/triage_digest.*
